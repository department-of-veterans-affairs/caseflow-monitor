<!DOCTYPE html>
<html lang="en">
<head>
  <title>Caseflow Monitor</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>

  <% if content_for?(:head) %><%= yield :head %><% end %>
</head>

<script>
  $(document).ready(function() {
    $("time.timeago").timeago();
  });
</script>

<body>
  <header>
    <nav class="cf-nav">
      <div class="usa-grid-full">
        <h1 class="cf-logo"><%= content_tag(:span, nil, :class => "cf-logo-image #{logo_class}") %>Caseflow</h1>
        <h2 id="page-title" class="cf-application-title">Monitor </h2>
          <div class="cf-dropdown cf-nav-dropdown cf-application-title">
            <% case ENV['DEPLOY_ENV'] when "demo" %>
              Demo
            <% when "uat" %>
              UAT
            <% when "preprod" %>
              Pre-Production
            <% when "spokepreprod" %>
              Pre-Production (Spoke)
            <% when "prod" %>
              Production
            <% end %>
            Environment
          </div>
      </div>
    </nav>
  </header>

  <main class="cf-app">
    <%= yield %>
  </main>

  <footer class="cf-txt-c usa-grid cf-app-footer">
    <div class="cf-push-left">
      <span>Built</span> with <abbr title="love">&#9825;</abbr> by the
      <a href="http://www.va.gov/ds/">Digital Service at the <abbr title="Department of Veterans Affairs">VA</abbr></a>.
    </div>
  </footer>
</body>
</html>
